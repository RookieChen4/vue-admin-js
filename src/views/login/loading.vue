<template>
  <div class="loader-container">
    <div class="loader" />
  </div>
</template>

<script>
export default {

}
</script>

<style lang="scss" scoped>
    .loader-container {
			width: 100%;
			height: 100%;
			display: flex;
			justify-content: center;
			align-items: center;
			min-height: 100vh;
			background: #000;
		}
		.loader {
			position: relative;
			width: 150px;
			height: 150px;
			background: #fff;
			border-radius: 50%;
			background: linear-gradient(45deg,transparent,transparent 40%,#e5f403);
			animation: animate 1s linear infinite;
		}
		@keyframes animate{
			0% {
				transform: rotate(0deg);
			}
			25% {
				transform: rotate(90deg);
				background: linear-gradient(45deg,transparent,transparent 40%,#3A9EEB);
			}
			50% {
				transform: rotate(180deg);
				background: linear-gradient(45deg,transparent,transparent 40%,#0CCD6C);
			}
			75% {
				transform: rotate(270deg);
				background: linear-gradient(45deg,transparent,transparent 40%,#4AC1AA);
			}
			100% {
				transform: rotate(360deg);
				background: linear-gradient(45deg,transparent,transparent 40%,#E665E9);
			}
		}
		.loader:before {
			content: '';
			position: absolute;
			top: 6px;
			left: 6px;
			right: 6px;
			bottom: 6px;
			background: #000;
			border-radius: 50%;
			z-index: 1000;
		}
		.loader:after {
			content: '';
			position: absolute;
			top: 0px;
			left: 0px;
			right:0px;
			bottom: 0px;
			background: #000;
			background: linear-gradient(45deg,transparent,transparent 40%,#e5f403);
			border-radius: 50%;
			filter: blur(30px);
			z-index: 1;
		}
</style>
